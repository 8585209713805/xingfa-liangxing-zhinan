# 2026-07-11 工作日志

## 非法经营罪量刑页 · 四条反馈修订（已推送）
用户就非法经营罪页提 4 条反馈，逐条处理：
1. **本罪特定从重/从宽进估算器 + 其他罪名**：核查全站 15 个罪名页，均已在估算器内置"本罪特定从重/从宽"（寻衅滋事罪用自定义"寻衅滋事·从重"区，非通用 #specHeavy 命名）——点 1 已全站满足。
2. **拘役为何不写"3年拘役"**：页内 §四 已用 ※ 框说明——刑法第42条拘役=1–6个月，不存在"3年拘役"；"3个月拘役–1年"下限为拘役、上限为1年。无需改。
3. **买卖外汇"四种情形降为250万/5万"未写清**：§三 立案追诉门槛③与 §四 表③均已加"（四种情形详见本节末脚注①②③④）"交叉引用；脚注列明法释〔2019〕1号第三条第二款四种情形（曾受刑究/二年内行政处罚/拒不交代资金去向/其他严重后果），已逐字核对正确。
4. **追溯门槛按序号分行**：§三 立案追诉门槛 19 类（烟草…其他兜底）改为 ①–⑲ 序号分行标注，便于检索。

## 非法经营罪量刑页 · 进一步修订（已推送）
用户再次反馈两点：
1. **删除 §四 黄色拘役说明框**：已删除 337 行附近的黄框 `※ 关于"拘役"`；保留 §一 速览卡片中的文字说明，但不再用高亮框，避免视觉干扰。
2. **"拘役"直接写清楚**：全页统一把"拘役–1年"改为"**3个月拘役–1年**"（含图例、§四 表所有类型、§五 基准刑增量说明、估算器按钮），并在速览卡片说明中直接标注"拘役（1个月–6个月）"，无需用户再查刑法第 42 条。
3. **新增"页面编辑模式"**：在工具栏增加"进入页面编辑模式 / 下载修改版 HTML"按钮，开启 `document.designMode` 后可在浏览器中直接 WYSIWYG 编辑整页文字与表格；点击下载会把当前输入值一并序列化为新 HTML 文件，用户可把修改版发给助手直接替换上传。

- 提交：`非法经营罪：删除§四黄色拘役说明框；拘役–1年统一为3个月拘役–1年；增加页面编辑模式与下载修改版功能`。

## 全站罪名页统一增加"页面编辑模式"
用户要求其他罪名页也支持"浏览器直接编辑→下载修改版→发我上传"。已通过脚本将非法经营罪页新增的编辑能力批量应用到其余 14 个罪名页：
- 工具栏加 `进入页面编辑模式` / `下载修改版 HTML` 两按钮；
- CSS 加 `body.page-edit .wrap` 虚线框高亮；
- JS 加 `togglePageEdit()`（切换 `document.designMode`）+ `downloadEditedPage()`（同步输入值后序列化整页下载，文件名取 `document.title + '-修改版.html'`）；
- 用 Python 脚本 `replace` 精准插入，已校验全站 15 页标签平衡、toggle/initEditor 各 1 处，提交并推送主分支。
- 工作流确定：**用户浏览器改 → 下载 HTML → 发我 → 我替换上传**，不再反复文字描述。

## 全站罪名页升级"一键上传到网站"（已推送）
用户希望把"下载修改版"升级为"一键上传到网站"。已实现**浏览器端直传 GitHub Pages**，无需服务器、无需再发文件给我：
- 工具栏新增绿色 `上传到网站` 按钮（常显）；保留 `下载修改版 HTML`（编辑模式下显）。
- 每个页面 `<head>` 注入 `<meta name="repo-path" content="crimes/<本文件名>">`，供 API 定位仓库内路径。
- JS 新增 `uploadToSite()`：首次点击 `prompt` 索取 GitHub PAT，存 `localStorage('gh_pat')`（可 `localStorage.removeItem('gh_pat')` 清除）；之后调用 GitHub Contents API `PUT /repos/8585209713805/xingfa-liangxing-zhinan/contents/{path}`（先 GET 取 sha，base64 编码整页后 PUT，commit message 含路径），并显示绿/红 toast 状态提示。
- 抽出 `serializeCurrentPage()` 供下载与上传共用（同步 input/select/textarea 值 + 去掉 page-edit 状态）。
- **安全提示**：PAT 仅存于用户本机浏览器 localStorage，建议用 fine-grained token 仅授权此仓库 Contents 读写；GitHub Pages 推送后约 1 分钟生效。
- 校验：全站 15 页 up=2（声明+调用）、ser=1、meta 均存在、标签平衡、initEditor 各 1 处；已提交推送主分支。

## 全站增加"编辑者模式"（仅本人可编辑，已推送）
用户要求：①未来所有罪名页都要带"编辑+上传"能力；②别人打开网站不能修改，只有本人能改。已实现：
- **权限门 `initOwner()`**：编辑/上传/下载三个按钮 + 说明 span 均加 `owner-only` class，CSS 默认 `display:none`；`body.is-owner` 时才显示。访客默认看到纯只读页面。
- **解锁方式**：本人用 `?owner=1` 打开任意页面一次 → 写 `localStorage('lx_owner')='1'` → 长期记忆；`?owner=0` 可注销编辑者身份。
- **安全边界**：真正写库仍需 GitHub Token（存 `localStorage('gh_pat')`，仅本机）。即便他人显示了按钮，无 token 也传不上去。已向用户说明用 fine-grained token 仅授权本仓库 Contents 读写。
- **未来罪名页自动带功能**：新页从样板克隆即继承（15 页样板均已含）；并把该"通用功能"要求写入 `assets/format_无依据罪名量刑页规范.md`（含所需 meta/toolbar/CSS/JS 清单 + 批量注入脚本 `/tmp/add_upload.py`、`/tmp/add_owner_gate.py` 参考）。
- 校验：全站 15 页 initOwner=1、调用=1、owner-only=7（4元素+3CSS）、标签平衡；已提交推送主分支。

## 隐私偏好：网址不暴露 GitHub 账号（暂维持现状）
用户明确不希望网址显示 GitHub 账号 `8585209713805`。已说明：GitHub Pages 默认域名 `用户名.github.io` 无法隐藏账号（平台硬限制），唯一解法是绑定用户自有自定义域名（CNAME + DNS + 仓库自定义域名设置 + Enforce HTTPS）。
- 2026-07-11 用户选择**暂不配置**，维持 `https://8585209713805.github.io/xingfa-liangxing-zhinan/` 现状。
- 未来若用户要求隐藏账号，直接走自定义域名方案（用户需先提供自有域名，再生成 CNAME 文件并配 DNS）。

## 赣高法〔2022〕115号 · 17个新罪名页已生成并推送上线
用户要求把赣高法〔2022〕115号《常见犯罪量刑指导意见（试行）实施细则（试行）》规定的罪名量刑页整理加入网站。
- 该文件共规定 23 罪；其中 6 罪（交通肇事/寻衅滋事/抢劫/故意伤害/盗窃/诈骗）此前已基于该文件生成，**仅新增 17 罪**：危险驾驶、非法吸收公众存款、集资诈骗、信用卡诈骗、合同诈骗、强奸、非法拘禁、抢夺、职务侵占、敲诈勒索、妨害公务、聚众斗殴、掩饰隐瞒犯罪所得、走私贩卖毒品、非法持有毒品、容留他人吸毒、引诱容留介绍卖淫。
- 17 页均从盗窃罪 A类模板克隆，内容按 docx 逐罪填充（量刑起点/基准刑增量/常见量刑情节/立案追诉门槛），含 owner-only 编辑上传工具栏与 `repo-path` meta，标题/编制说明/§五 均标注 2022 文件依据。
- `assets/crimes_data.js` 索引：链接页面 15 → **32**，缺失=NONE。
- 提交 `317e8ac` 已 `git push origin HEAD` 成功（`96f9600..317e8ac HEAD -> main`）。推送曾因沙箱代理对 `github.com:443` 瞬发 502 失败，重试后恢复；验证 Pages 首页与 危险驾驶罪 新页均返回 200，已上线。
- 站点现共 32 个可链接罪名页；网址不变 `https://8585209713805.github.io/xingfa-liangxing-zhinan/`；本人用 `?owner=1` 可编辑/上传。

